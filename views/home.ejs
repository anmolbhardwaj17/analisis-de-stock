<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <link rel="stylesheet" href="stylesheets/home.css">
    <title>Home</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.1/font/bootstrap-icons.css">
</head>
<body>
    <div class="outer">
        <div class="header">
            <h1>Crypto Analysis</h1>
            <p style="color:#818181">Get crypto data on the go</p> 
        </div>
        <div class="main">
            <div class="card-header">
                <div class="card1 bold">
                    <p>Symbol</p>
                    <p> Name</p>
                </div>
                <div class="card2 bold">
                    <p>Value</p>
                    <p>24hr</p>
                </div>
            </div>
            <%  for(let i=0;i<data.length;i++){ %>
                <div onclick="location.href='/<%= data[i].id %>';" class="card">
                    <div class="card1">
                        <p class="symbol"><%= data[i].symbol %></p>
                        <p><%= data[i].name.substring(0, 12) %></p>
                    </div>
                    <div class="card2">
                        <p>$<%= parseInt(data[i].priceUsd).toFixed(2) %></p>
                        <% if(data[i].changePercent24Hr > 0) {%>
                            <p><i class="bi bi-arrow-up-short green-stock"></i><%= parseFloat(data[i].changePercent24Hr).toFixed(2) %>%</p>
                            <% } else{ %>
                            <p><i class="bi bi-arrow-down-short red-stock"></i><%= parseFloat(data[i].changePercent24Hr).toFixed(2) %>%</p>
                            <% } %>
                    </div>
                </div>
            <%}%>
            
        </div>
    </div>
</body>

</html>